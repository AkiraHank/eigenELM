{
  "options": {
    "cwd": "${workspaceFolder}"
  },
  "tasks": [
    {
      "type": "shell",
      "label": "build",
      "command": "cd ${workspaceFolder}/build; cmake ..; make",
      "args": [
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ],
  "version": "2.0.0"
}